{
  "urls": [
    [
      "board_config.py",
      "github:PyDevices/pydevices/board_configs/sdldisplay/linux_kms/board_config.py"
    ]
  ],
  "deps": [
    [
      "pydevices-desktop",
      "latest"
    ]
  ],
  "version": "0.1",
  "notes": "Linux KMS/DRM via SDL_VIDEODRIVER=kmsdrm (no X11/Wayland). Requires libsdl2 built with kmsdrm and access to /dev/dri."
}
